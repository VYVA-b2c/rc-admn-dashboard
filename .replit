modules = ["nodejs-20"]

run = "npm run dev -- --host 0.0.0.0 --port 8080"

[deployment]
deploymentTarget = "cloudrun"
build = ["sh", "-c", "npm run build"]
run = ["sh", "-c", "npm run preview -- --host 0.0.0.0 --port ${PORT:-3000}"]
